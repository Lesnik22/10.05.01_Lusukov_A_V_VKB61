# Безопасная корпоративная система обмена файлами

Система для безопасного обмена файлами в корпоративной среде с использованием многофакторной аутентификации (MFA) и ролевой модели доступа (RBAC).

## 🚀 Возможности

### Безопасность
- **Многофакторная аутентификация (MFA)** - поддержка TOTP через Google Authenticator, Authy и другие приложения
- **Шифрование файлов** - все файлы шифруются перед сохранением
- **Ролевая модель доступа (RBAC)** - гибкая система ролей и разрешений
- **Аудит действий** - полное логирование всех операций пользователей

### Управление файлами
- **Безопасная загрузка** - файлы шифруются при загрузке
- **Контроль доступа** - настройка уровней доступа (приватный, отдел, публичный)
- **Предоставление доступа** - возможность делиться файлами с другими пользователями
- **Безопасное скачивание** - файлы расшифровываются только при скачивании

### Администрирование
- **Панель администратора** - управление пользователями и мониторинг системы
- **Журнал аудита** - отслеживание всех действий пользователей
- **Статистика безопасности** - мониторинг использования MFA и активности

## 🛠 Технологии

- **Backend**: Python 3.8+, Flask
- **База данных**: SQLite (разработка), PostgreSQL (продакшен)
- **Аутентификация**: Flask-Login, MFA через pyotp
- **Шифрование**: ГОСТ 34.12-2015 "Кузнечик" (gost_crypto)
- **Frontend**: Bootstrap 5, HTML5, JavaScript
- **Безопасность**: bcrypt для хеширования паролей

## 📋 Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## 🔧 Установка

1. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/Mac
   source venv/bin/activate
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте переменные окружения (`KEY.env`):**
   
   Проект загружает настройки из файла `KEY.env` (см. `config.py`).
   
   Минимально обязательная настройка:
   - `ENCRYPTION_KEY` — мастер-ключ шифрования (без него приложение не стартует).
   
   Пример `KEY.env`:
   ```env
   # обязательно
   ENCRYPTION_KEY=<ваш_ключ_ГОСТ_32_байта_base64url>

   # опционально (VirusTotal)
   VT_API_KEY=<ваш_api_key>

   # опционально (локальный HTTPS по IP)
   ENABLE_TLS=1
   TLS_CERT_PATH=certs/dev-cert.pem
   TLS_KEY_PATH=certs/dev-key.pem
   ```
   
   Как сгенерировать `ENCRYPTION_KEY`:
   ```bash
   python -c "from gost_crypto import GOSTKuznyechik; import base64; key = GOSTKuznyechik.generate_key(); print(base64.urlsafe_b64encode(key).decode())"
   ```

4. **(Опционально) Включите локальный HTTPS (TLS) по IP:**
   
   Если вы хотите открывать приложение по адресу вида `https://192.168.x.x:5000/`,
   сгенерируйте самоподписанный сертификат с SAN под ваш IP:
   ```bash
   python scripts/generate_dev_tls_cert.py --ip <ваш_ip>
   ```
   
   После этого откройте:
   - Локально: `https://127.0.0.1:5000`
   - По сети: `https://<ваш_ip>:5000`
   
   Примечание: браузер покажет предупреждение, т.к. сертификат самоподписанный (это нормально для локальной сети).
   Чтобы вернуть HTTP, установите `ENABLE_TLS=0` (или удалите переменную).

5. **Инициализируйте базу данных / первый запуск:**
   ```bash
   python app.py
   ```

6. **Запустите приложение:**
   ```bash
   python app.py
   ```

Приложение будет доступно по адресу:
- **HTTP**: `http://127.0.0.1:5000` (если `ENABLE_TLS=0`)
- **HTTPS**: `https://127.0.0.1:5000` и `https://<ваш_ip>:5000` (если `ENABLE_TLS=1`)

## 👤 Демо-аккаунт

Для тестирования системы используйте:
- **Логин**: `admin`
- **Пароль**: `admin123`

## 🔐 Настройка MFA

1. Войдите в систему
2. Перейдите в настройки профиля
3. Нажмите "Настройка MFA"
4. Отсканируйте QR-код в приложении аутентификатора
5. Введите код для подтверждения

## 🏗 Архитектура

### Модели данных

- **User** - пользователи системы
- **Role** - роли с набором разрешений
- **File** - файлы с метаданными
- **FileAccess** - права доступа к файлам
- **AuditLog** - журнал аудита

### Роли по умолчанию

- **admin** - полный доступ к системе
- **manager** - управление файлами и пользователями
- **user** - базовые права на загрузку и скачивание

### Уровни доступа к файлам

- **private** - только владелец
- **department** - пользователи отдела
- **public** - все пользователи

## 🔒 Безопасность

### Шифрование
- Все файлы шифруются с использованием ГОСТ 34.12-2015 "Кузнечик" (блочный шифр, 256 бит ключ)
- Ключи шифрования генерируются автоматически
- Файлы расшифровываются только при скачивании

### Аутентификация
- Пароли хешируются с использованием bcrypt
- Поддержка MFA через TOTP
- Сессии с настраиваемым временем жизни

### Аудит
- Логирование всех действий пользователей
- Отслеживание IP-адресов и User-Agent
- Хранение детальной информации о операциях

## 📊 Мониторинг

### Админ панель
- Статистика пользователей и файлов
- Мониторинг использования MFA
- Просмотр журнала аудита
- Управление пользователями

### Метрики безопасности
- Количество активных пользователей
- Статистика использования MFA
- Анализ активности пользователей
- Отчеты по безопасности

## 🚀 Развертывание

### Продакшен

1. **Настройте переменные окружения:**
   ```bash
   export FLASK_ENV=production
   export SECRET_KEY=your-secret-key
   export DATABASE_URL=postgresql://user:password@localhost/db
   ```

2. **Используйте WSGI сервер:**
   ```bash
   pip install gunicorn
   gunicorn -w 4 -b 0.0.0.0:8000 app:app
   ```

3. **Настройте обратный прокси (nginx):**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://127.0.0.1:8000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

### Docker

```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
EXPOSE 5000

CMD ["python", "app.py"]
```

## 🧪 Тестирование

```bash
# Запуск тестов
python -m pytest tests/

# Покрытие кода
python -m pytest --cov=app tests/
```

## 📝 API Документация

### Аутентификация
- `POST /login` - вход в систему
- `POST /register` - регистрация пользователя
- `GET /logout` - выход из системы

### Файлы
- `POST /upload` - загрузка файла
- `GET /download/<file_id>` - скачивание файла
- `POST /share_file/<file_id>` - предоставление доступа

### MFA
- `GET /setup_mfa` - настройка MFA
- `POST /enable_mfa` - включение MFA
- `GET /mfa_verify/<user_id>` - проверка MFA

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте документацию
2. Создайте Issue в репозитории
3. Обратитесь к администратору системы

## 🔄 Обновления

### Версия 1.0.0
- Базовая функциональность системы
- MFA аутентификация
- Ролевая модель доступа
- Шифрование файлов
- Админ панель
- Аудит действий

---

**Важно**: В продакшене обязательно измените SECRET_KEY и настройте HTTPS для обеспечения безопасности.

Также важно: не публикуйте `KEY.env` (в нем находятся секреты и ключи).

